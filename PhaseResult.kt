package plugin.koms.calculator

class PhaseResult {
    var time = 0.0
    var distance = 0.0
    var hp = 0.0
    override fun toString(): String {
        return "PhaseResult(time=$time, distance=$distance, hp=$hp)"
    }
}